# Where ydotool looks for ydotoold's socket. The packaged unit is system-wide
# and puts its socket somewhere the session cannot reach, so a user-level
# daemon owns it instead.
YDOTOOL_SOCKET=${XDG_RUNTIME_DIR}/.ydotool_socket
